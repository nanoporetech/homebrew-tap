cask "{{NAME}}" do
  version "{{VERSION}}"
  sha256 "{{SHA256}}"

  url "{{URL}}"
  name "EPI2ME Labs Launcher ({{NAME}})"

  homepage "https://labs.epi2me.io"

  uninstall delete: "#{HOMEBREW_PREFIX}/bin/{{NAME}}"
end
